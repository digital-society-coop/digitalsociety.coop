name: dev

on:
  push:
    branches:
      - '**'
      - '!main'

permissions:
  contents: read

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yaml
    with:
      environment: dev
    secrets: inherit
